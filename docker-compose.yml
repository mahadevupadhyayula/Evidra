services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: evidra
      POSTGRES_USER: evidra
      POSTGRES_PASSWORD: evidra
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
